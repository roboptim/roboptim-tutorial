SET(EXAMPLE_NAME 002-operator)
ADD_EXECUTABLE(${EXAMPLE_NAME} ${EXAMPLE_NAME}.cc)

ADD_TEST(${EXAMPLE_NAME} ${RUNTIME_OUTPUT_DIRECTORY}/${EXAMPLE_NAME})
PKG_CONFIG_USE_DEPENDENCY(${EXAMPLE_NAME} roboptim-core)
